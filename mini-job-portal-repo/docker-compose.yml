version: '3.9'
services:
  backend:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - ./backend:/app
    command: sh -c "npm install && npm start"
    ports:
      - "4000:4000"
    env_file:
      - ./backend/.env.example
